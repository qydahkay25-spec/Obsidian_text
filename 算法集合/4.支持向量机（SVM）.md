# 支持向量机（SVM）
1. 定义：一种监督学习算法，在分类任务中表现优异
2. 核心思想：找到一个最优超平面，最大化分类间隔
3. 状态：
	线性可分→硬间隔
	存在一个超平面，能够将不同类别的样本完全正确划分。
	线性不可分→软间隔
	允许部分样本违反分类间隔，通过惩罚因子C 控制对违反约束样本的容忍度
4. 核函数
	当数据在原始空间线性不可分时，通过核函数将样本映射到更高维空间，使其在高维空间中线性可分。